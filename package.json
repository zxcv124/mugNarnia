{
  "name": "mycv",
  "version": "0.0.0",
  "scripts": {
    "start": "npm run dev",

    "dev": "vite",
    "build": "vite build",
    "preview": " npm run build && vite preview --port 80"
  },
  "devDependencies": {
    "vite": "^2.7.2"
  },
  "dependencies": {
    "nodemon": "^2.0.15",
    "three": "^0.136.0"
  }
}
